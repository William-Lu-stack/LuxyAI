# Public multi-architecture images routed through DaoCloud's mainland mirror.
# Keep immutable tags in production; avoid `latest` so mirror caches stay deterministic.
image:
  repository: m.daocloud.io/ghcr.io/your-org/flawless
  tag: 3.2.2
  pullPolicy: IfNotPresent

imagePullSecrets: []

config:
  NODE_EXEC_IMAGE: m.daocloud.io/ghcr.io/your-org/flawless-node-exec:1.36
